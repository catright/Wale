<Window x:Class="CoreTest.CoreTestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoreTest"
        mc:Ignorable="d" Closing="Window_Closing"
        Title="CSCore Test" Height="250" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="{Binding Name}"/>
            <TextBlock Text="{Binding MMD}"/>
            <TextBlock Text="{Binding MSD.Muted}"/>
            <Button Content="Vol" Click="VolButton_Click"/>
            <TextBlock Text="{Binding MSD.Volume}"/>
            <TextBlock Text="{Binding MSD.VolumedB}"/>
            <TextBlock Text="{Binding MSD.Peak}"/>
            <TextBlock Text="{Binding MSD.PeakdB}"/>
        </StackPanel>
        <Grid Grid.Column="1">
            <!--<ListView ItemsSource="{Binding SSD}" DisplayMemberPath="DisplayName"/>-->
            <!--<ListView ItemsSource="{Binding SSN}"/>-->
            <StackPanel x:Name="SSPanel"/>
        </Grid>
    </Grid>
</Window>

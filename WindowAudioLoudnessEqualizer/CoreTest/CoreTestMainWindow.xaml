<Window x:Class="CoreTest.CoreTestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoreTest"
        mc:Ignorable="d" Closing="Window_Closing"
        Title="CSCore Test" Height="250" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="{Binding Name}"/>
            <TextBlock Text="{Binding MMD}"/>
            <TextBlock Text="{Binding MD.Muted}"/>
            <Button Content="Vol" Click="VolButton_Click"/>
            <TextBlock Text="{Binding MD.Volume}"/>
            <TextBlock Text="{Binding MD.VolumedB}"/>
            <TextBlock Text="{Binding MD.Peak}"/>
            <TextBlock Text="{Binding MD.PeakdB}"/>
        </StackPanel>
        <Grid Grid.Column="1">
            <!--<ListView ItemsSource="{Binding Ss}" DisplayMemberPath="Volume"/>-->
            <ListView ItemsSource="{Binding SNames}"/>
        </Grid>
    </Grid>
</Window>
